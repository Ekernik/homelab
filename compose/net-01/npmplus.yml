services:
  npmplus:
    container_name: npmplus
    image: docker.io/zoeyvid/npmplus:latest
    restart: unless-stopped
    ports:
      - '81:81/tcp' # NPMPlus web console (HTTP)
      - '80:80'
      - '443:443'
    volumes:
      - /mnt/configs/npmplus:/data
    env_file: 'npmplus.env'
